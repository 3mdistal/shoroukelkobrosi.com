interface TimelineValues {
  fadeDuration: number
  titleDelay: number
  subtitleDelay: number
  reelDelay: number
}

export const timelineValues: TimelineValues = {
  fadeDuration: 0.6,
  titleDelay: 0.5,
  subtitleDelay: 1,
  reelDelay: 1.5,
}
