---
title: "This is the beginning of my markdown file."
publicationDate: "2024-05-20"
---

## {frontmatter.title}
<p>{frontmatter.publicationDate}</p>


I'm testing to see how this works, because if it's just import and go, I'm so fucking pumped.

```ts
const test = "test";

function testFunction() {
  console.log(test);
}
```

The above is a code sample, just to see if there's inbuilt syntax highlighting, of course.